logging:
  level:
    org.springframework.data.mongodb.core: DEBUG
    org.mongodb.driver: DEBUG

migration:
  enabled: true
  directory: db/migration
  plans:
    referential:
      enabled: true
      commands:
        - REMOVE_ALL
        - INSERT
      includes:
        - countries.json
        - products.json

